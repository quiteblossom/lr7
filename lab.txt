#include <iostream>
#include "windows.h"

using namespace std;

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);

	float origNum, reverseNum, a, b;
	cout << "Введіть число: ";
	cin >> origNum;
	a = (int)origNum % 10;
	b = (int)origNum / 10;
	reverseNum = a * 100 + b;
	cout << "Перевернуте число: " << reverseNum;
}
